coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        informational: true

comment:
  layout: 'reach,diff,flags,tree'
  behavior: default
  require_changes: false
